# SPA fallback — Cloudflare Pages still serves real static files first when they
# exist. Do not rewrite /assets/* (a self-rewrite can mis-handle missing files
# and return this shell as text/html for JS modules).
/*
  /index.html
  200
