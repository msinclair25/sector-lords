# Next Steps

## 1. GitHub repo (next)

Repo hygiene is ready:

- [x] `.gitignore` (node_modules, dist, wrangler, traces, Obsidian workspace)
- [x] `.gitattributes`, `.editorconfig`, `.npmrc`
- [x] `AGENTS.md` + `CONTRIBUTING.md`
- [x] README hybrid-first + live URL + docs pointer

Still to do when you say go:

- [ ] `git init` (if needed) + create GitHub repo
- [ ] Decide public vs private
- [ ] Decide: full `public/assets` in git vs LFS / release zip
- [ ] LICENSE if public
- [ ] Initial commit + push

**Suggested remote name:** `sector-lords`  

Do **not** force-push or rewrite history on a shared remote without saying so.

## 2. Product backlog (when interested)

- Low-end laptop performance capture → analyze vs desktop trace
- Tech drawer: close on order-guide / field orders
- itch.io page
- Custom domain if traction
- Further HUD incremental render (perf) only if lag appears

## 3. Docs habit

After each meaningful ship:

1. Entry in [[Progress Log]]
2. Decision in [[Decisions]] if it was a tradeoff
3. Note in [[Grok Build Evaluation]] if it taught something about the tool
